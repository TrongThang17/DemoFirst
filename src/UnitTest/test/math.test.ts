export const MathJS = require('../math')

it('Adds 1 + 1 to equals 2', () => {
    expect(MathJS.sum(1, 1)).toBe(2);
  });
  it('Adds 1 + 1 to equals 2', () => {
    expect(MathJS.sum(1, 1)).toBe(2);
  });
  it('Adds 1 + 1 to equals 2', () => {
    expect(MathJS.sum(1, 1)).toBe(2);
  });

